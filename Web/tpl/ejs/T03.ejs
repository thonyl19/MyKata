<% 
	for(var idx in users){
		var _msg = `${idx} = ${JSON.stringify(users[idx])},\n`;
		echo(_msg);
	}
-%>
<%= users %>
<% for(var idx in users){ _%>
	<%# 使用未轉義輸出  %><%- JSON.stringify(users[idx]) %>
<% } %>

 