/*
<% for(var item of Src.Fileds) { _%>
	<%= item.label%>
<%_ } _%>
*/
var SID = <%= Src.SID_Filed%>;
var isAdd = string.IsNullOrEmpty(SID);
if (!isAdd) {
	var chk = CheckOldRecord(SID);
	if (!chk.Success) return chk;
	ReturnData = chk.Data;
}
