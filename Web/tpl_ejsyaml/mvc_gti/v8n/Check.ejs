/*
<% for(var item of arg.Fileds) { _%>
	<%= item.label%>
<%_ } _%>
*/
var SID = <%= arg.SID_Filed%>;
var isAdd = string.IsNullOrEmpty(SID);
if (!isAdd) {
	var chk = CheckOldRecord(SID);
	if (!chk.Success) return chk;
	ReturnData = chk.Data;
}
