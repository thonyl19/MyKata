c_val: {
    get() {
        var _ver = this.get_ver(this.c_SID);
        if (_ver != null && !_.isEqual(_ver, this.value)) {
            this.c_val = _ver;
        }
        return _ver || {};
    },
    set(val) {
        this.$emit('input', val);
        this.<%=Src.Grp.TriggerEvent%>();
    }
},